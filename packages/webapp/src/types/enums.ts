/**
 * `Routes` defines the set of pathnames used for navigation within the application.
 * This enum is utilized by the Next.js router to manage navigation links throughout the application,
 * ensuring that pathnames are consistent and easily refactorable.
 */
export enum Routes {
    Home = "/",
    Connect = "/connect",
    Feed = "/feed",
}
